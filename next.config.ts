import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // output: "export",   // bắt buộc để export ra static HTML
  // images: {
  //   unoptimized: true, // tránh lỗi tối ưu ảnh trên GitHub Pages
  // },
  // basePath: "/bna",     // thay bằng tên repo GitHub của bạn
  // assetPrefix: "/bna/", // prefix cho assets
};

export default nextConfig;
